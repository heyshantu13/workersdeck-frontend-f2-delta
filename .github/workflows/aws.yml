name: Deploy Workers Deck

on:
  push:
    branches:
    - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v1

    - name: Configure AWS Credentials
      uses: aws-actions/configure-aws-credentials@v1
      with:
        aws-access-key-id: AKIA6NP4DNY6OJ7DMB4D
        aws-secret-access-key: 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
        aws-region: us-east-2

    - name: Deploy static site to S3 bucket
      run: aws s3 sync ./public/ s3://workersdeck-fe --delete